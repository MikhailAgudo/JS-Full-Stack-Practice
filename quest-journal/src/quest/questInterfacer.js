const questInterfacer = (() => {
    const strucTitle = (title) => {
        title = String(title);
        return title;
    }
    return {
        strucTitle
    }
})();

export {
    questInterfacer
}